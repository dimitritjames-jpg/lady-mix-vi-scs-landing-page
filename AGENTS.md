# AGENTS.md

## Cursor Cloud specific instructions

Next.js landing page. Node deps are installed automatically on VM startup.

- Dev server: `npm run dev` (port 3000; pass `-- --port <n>` to change). No env vars required.
- Lint: `npm run lint` (`next lint`). Build: `npm run build`.
